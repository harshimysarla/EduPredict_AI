[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "edupredict-ai"
version = "1.0.0"
requires-python = ">=3.10,<3.13"

[tool.fastapi]
entrypoint = "backend.app.main:app"

[tool.vercel]
entrypoint = "backend.app.main:app"
